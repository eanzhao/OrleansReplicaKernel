# AGENTS

## 项目定位

`OrleansReplicaKernel` 的目标不是做一个故意缩小、故意简化的 toy runtime。

这个项目的完整目标，是在新的架构边界和实现组织下，完整复刻 Orleans。
当前代码库还处在早期阶段，只是已经先做出了一部分内核主链和验证原型，但离“完整复刻 Orleans”还有明显距离。

## 编写和改动时的约束

1. 不要把“现在还没做到”写成“项目不打算做”。
2. 不要把“当前实现范围”误写成“最终产品边界”。
3. 写文档时，要明确区分：
   - 已做到的部分
   - 计划做到、但现在还没做到的部分
4. 做设计判断时，要优先服务“最终完整复刻 Orleans”这个目标，而不是把当前原型永久化。
5. 如果要描述当前代码，请优先使用“阶段性实现”“当前进度”“现阶段覆盖范围”这类表达，避免再把它称作“故意做小的 toy”。
